<Window x:Class="KillerPDF.MainWindow" Icon="pack://application:,,,/Resources/kp-icon.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KillerPDF"
        mc:Ignorable="d"
        Title="KillerPDF" Height="700" Width="1000" MinHeight="480" MinWidth="620"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource BgDark}"
        FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <!-- Hardware-accelerated custom chrome. Dropping AllowsTransparency lets WPF composite the
         whole window on the GPU again (the film grain + drop shadows were being software-rendered
         every frame, which made dragging/animation jumpy). WindowChrome gives native drag, snap,
         resize and the OS drop shadow; the title bar (row 0, 36px) becomes the caption and the
         min/max/close buttons opt back into hit-testing with IsHitTestVisibleInChrome. -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="36"
                      ResizeBorderThickness="8"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      NonClientFrameEdges="None"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Brush keys come from Application.Resources (theme dicts) via DynamicResource.
             Only non-color styles live here. -->

        <!-- Floors the scrollbar thumb's proportional size so it stays grabbable on long docs. -->
        <local:ThumbViewportFloorConverter x:Key="ThumbFloor"/>

        <!-- Close button's top-right corner: rounded to match the window when floating, squared
             when maximized/snapped. Updated in UpdateWindowChrome alongside the frame corners. -->
        <CornerRadius x:Key="ChromeCloseCorner">0,7,0,0</CornerRadius>

        <!-- Shared film-grain brush for popup menus (settings/shortcuts/language/overflow).
             ImageSource is assigned once in ApplyGrainTexture; every reference updates. -->
        <ImageBrush x:Key="GrainBrushShared" TileMode="Tile"
                    ViewportUnits="Absolute" Viewport="0,0,256,256" Stretch="None"/>

        <!-- Settings section subheaders: white with a faint depth shadow (the "Settings" title carries the
             accent colour instead). Margin stays inline per row. -->
        <Style x:Key="SettingsSubheader" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Direction="270" Opacity="{DynamicResource HeaderShadowOpacity}"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Settings current-value labels (English, Dark, ...): accent-coloured, with a depth shadow so the
             accent stays legible on every theme background. -->
        <Style x:Key="SettingsAccentValue" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource HeaderAccent}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Direction="270" Opacity="{DynamicResource HeaderShadowOpacity}"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Overflow menu row button -->
        <Style x:Key="OverflowMenuButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="3" Padding="8,5">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource BgPanel}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toolbar button -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.Effect>
                                    <!-- IconShadowOpacity is 0 in Light theme, so the glyph stays crisp ClearType
                                         there; dark themes get the soft depth shadow. -->
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                </ContentPresenter.Effect>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToolbarButtonAccent" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{DynamicResource AccentLogo}"/>
        </Style>
        <!-- Main half of an Open/Save split button: its hover fill is inset on the right so it
             stops just short of the overlapping dropdown chevron instead of running under it. -->
        <Style x:Key="ToolbarSplitMain" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="bg" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3" Margin="0,0,7,0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              ToolTip="{TemplateBinding ToolTip}">
                                <ContentPresenter.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                </ContentPresenter.Effect>
                            </ContentPresenter>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToolbarSplitMainAccent" TargetType="Button" BasedOn="{StaticResource ToolbarSplitMain}">
            <Setter Property="Foreground" Value="{DynamicResource AccentLogo}"/>
        </Style>
        <Style x:Key="ToolbarButtonDanger" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
        </Style>
        <Style x:Key="ToolbarToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                </ContentPresenter.Effect>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectionBg}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SelectionFg}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ChromeCloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
            <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource ChromeCloseCorner}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DangerRed}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab close button: themed so hover is the danger red (not WPF's default blue chrome). -->
        <Style x:Key="TabCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DangerRed}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Shared close "X" for overlay headers, recent-list rows, and the signature box. Glyph turns
             danger red with a soft drop shadow on hover, and a slightly deeper shadow when pressed.
             Centralised so every close X behaves identically. -->
        <Style x:Key="DangerCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="Cp" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
                                <Setter TargetName="Cp" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource DangerRed}"/>
                                <Setter TargetName="Cp" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="2" Direction="270" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- New-tab "+" button: subtle hover, no default chrome. -->
        <Style x:Key="TabNewButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Resize grip: small dotted triangle, inset from the corner so it clears the rounded
             window corner. The control keeps its hit area, only the dots are pulled in. -->
        <Style TargetType="ResizeGrip">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ResizeGrip">
                        <Grid Background="Transparent" SnapsToDevicePixels="True" Margin="0,0,5,5">
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,0"/>
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,4,0"/>
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,8,0"/>
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,4,4"/>
                            <Rectangle Width="2" Height="2" Fill="{DynamicResource TextDim}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Thin green scrollbar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="MinWidth" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True"
                                   Minimum="{TemplateBinding Minimum}"
                                   Maximum="{TemplateBinding Maximum}"
                                   Value="{TemplateBinding Value}">
                                <!-- Reactive thumb with a minimum-length floor. ViewportSize is fed through
                                     ThumbViewportFloorConverter, which raises it just enough that the Track's
                                     proportional thumb never falls below the floor. Because the Track sizes the
                                     thumb AND the repeat buttons from this one value, there is no MinHeight
                                     overflow for the increase button to paint over. The buttons still enable
                                     click-to-page. -->
                                <Track.ViewportSize>
                                    <MultiBinding Converter="{StaticResource ThumbFloor}" ConverterParameter="64">
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ViewportSize"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Maximum"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Minimum"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualHeight"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Orientation"/>
                                    </MultiBinding>
                                </Track.ViewportSize>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <!-- No MinHeight/MinWidth here on purpose: the thumb's minimum length is
                                         enforced upstream via the Track's floored ViewportSize (see above), so
                                         the thumb's rendered size always equals the slot the Track gives it.
                                         A MinHeight here would overflow that slot and be covered by the
                                         increase button - the original tiny-scrollbar bug. -->
                                    <Thumb x:Name="ScrollThumb">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border x:Name="thumbBorder"
                                                                    Background="{DynamicResource BgScrollThumb}"
                                                                    CornerRadius="3"
                                                                    Margin="4,0"/>
                                                            <ControlTemplate.Triggers>
                                                                <!-- Horizontal bar: keep the thumb thin on the vertical axis instead -->
                                                                <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}" Value="Horizontal">
                                                                    <Setter TargetName="thumbBorder" Property="Margin" Value="0,4"/>
                                                                </DataTrigger>
                                                                <!-- Fatten + highlight whenever the mouse is anywhere over the scrollbar.
                                                                     Only the thumb's margin changes, so the reserved width stays constant
                                                                     and the page does not reflow/re-render on hover. -->
                                                                <!-- Only react over the thumb itself, not anywhere along the track. -->
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ScrollThumbHover}"/>
                                                                    <Setter TargetName="thumbBorder" Property="Margin" Value="2,2"/>
                                                                    <Setter TargetName="thumbBorder" Property="Effect">
                                                                        <Setter.Value>
                                                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0" Opacity="0.3"/>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="12"/>
                    <Setter Property="MinHeight" Value="12"/>
                    <Setter Property="Width" Value="Auto"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Themed slider (size/opacity bars): groove + thumb that match the scrollbar palette.
             OverridesDefaultStyle so none of WPF's default blue track/selection can show through. -->
        <Style x:Key="DarkSliderRepeat" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkSliderThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <!-- Solid at rest (the scrollbar's active color) so it's always visible,
                             brightening to the accent while hovered/dragged. -->
                        <Border x:Name="tb" Background="{DynamicResource ScrollThumbHover}" CornerRadius="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tb" Property="Background" Value="{DynamicResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="tb" Property="Background" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkSlider" TargetType="Slider">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center" Background="Transparent">
                            <Border Height="4" CornerRadius="2" VerticalAlignment="Center"
                                    Background="{DynamicResource SliderTrack}"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource DarkSliderRepeat}" Command="{x:Static Slider.DecreaseLarge}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource DarkSliderThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource DarkSliderRepeat}" Command="{x:Static Slider.IncreaseLarge}"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark context menu -->
        <!-- Themed menu separator: a thin dim line instead of the bright default. -->
        <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Margin="8,4" Background="{DynamicResource BorderDim}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource BgModal}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <!-- Margin leaves room for the drop shadow to render inside the popup. -->
                        <Grid Margin="5">
                            <!-- Shadow-only layer behind, so the menu text stays crisp. -->
                            <Border Background="{TemplateBinding Background}" CornerRadius="4" IsHitTestVisible="False">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="2" Direction="270" Opacity="0.5"/>
                                </Border.Effect>
                            </Border>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4" Padding="4">
                                <Grid>
                                    <!-- Film grain on the menu surface, matching the settings panel. -->
                                    <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}"
                                            CornerRadius="4" Margin="-4" Background="{StaticResource GrainBrushShared}"/>
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="20,6,16,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuBd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}"
                                           Foreground="{DynamicResource TextSecondary}" FontSize="11"
                                           VerticalAlignment="Center" Margin="24,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuBd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource BorderDim}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,4"/>
        </Style>

        <!-- ======================================================== -->
        <!-- Dark ComboBox (zoom dropdown)                           -->
        <!-- ======================================================== -->

        <!-- Toggle button used inside the ComboBox template -->
        <Style x:Key="DarkComboToggle" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem inside the dark popup -->
        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource SelectionBg}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The ComboBox itself -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{DynamicResource BgPanel}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Clickable area / border -->
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Selected item text (non-editable mode) -->
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="7,0,0,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"/>
                                    <!-- Editable text box (shown when IsEditable=True) -->
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="5,0,0,0"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource TextPrimary}"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             VerticalAlignment="Center"
                                             IsReadOnly="{TemplateBinding IsReadOnly}"
                                             Visibility="Hidden"
                                             Focusable="True"
                                             SelectionBrush="{DynamicResource AccentDim}"
                                             SelectionTextBrush="{DynamicResource Accent}"
                                             CaretBrush="{DynamicResource Accent}"/>
                                    <!-- Chevron arrow -->
                                    <TextBlock Grid.Column="1"
                                               Text="&#xE70D;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="8"
                                               Foreground="{DynamicResource TextSecondary}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               IsHitTestVisible="False"/>
                                    <!-- Invisible toggle over the whole control -->
                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2"
                                                  Style="{StaticResource DarkComboToggle}"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Grid>
                            </Border>
                            <!-- Dropdown popup -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   Placement="Bottom">
                                <Border Background="{DynamicResource BgPanel}"
                                        BorderBrush="{DynamicResource BorderDim}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="2"
                                        Margin="0,2,6,6"
                                        MinWidth="{Binding ActualWidth,
                                                   RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="220">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="2" Direction="270" Opacity="0.5"/>
                                    </Border.Effect>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Page list styles -->
        <Style x:Key="PageListBox" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderDim}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectionBg}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar tab buttons (PAGES / OUTLINES) -->
        <Style x:Key="SidebarTabBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="2,2,8,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark TreeView for PDF outline/bookmarks -->
        <Style x:Key="OutlineTreeStyle" TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="OutlineItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="IsExpanded" Value="False"/>
            <Setter Property="Padding" Value="2,3"/>
        </Style>

        <!-- General-purpose themed text button -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BgPanel}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Settings panel radio button -->
        <!-- Clickable accent-color swatch for the Dark theme family (website-style picker).
             Literal hue colors, identical across themes. The selection ring is set in code. -->
        <Style x:Key="AccentDot" TargetType="Border">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="CornerRadius" Value="9"/>
            <Setter Property="Margin" Value="0,0,7,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="ThemeRadio" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="Transparent" CornerRadius="3"
                                Margin="-8,0,2,0" Padding="10,4,10,4">
                            <!-- DockPanel (not a horizontal StackPanel) so the content fills the row
                                 width, letting a right-aligned column (e.g. language codes) reach the
                                 right edge. -->
                            <DockPanel LastChildFill="True" Background="Transparent">
                                <Grid Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0" DockPanel.Dock="Left">
                                    <Ellipse x:Name="ring" Width="14" Height="14"
                                             Stroke="{DynamicResource TextDim}" StrokeThickness="1.5"
                                             Fill="Transparent"/>
                                    <Ellipse x:Name="dot" Width="7" Height="7"
                                             Fill="{DynamicResource RadioAccent}" Visibility="Collapsed"/>
                                </Grid>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource RadioAccent}"/>
                                <Setter Property="Foreground" Value="{DynamicResource RadioAccent}"/>
                                <Setter TargetName="bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Direction="270" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource RadioAccent}"/>
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Form-field text box: a minimal template so the field shows OUR border/fill (transparent
             at rest, accent on focus) instead of the stock WPF blue focus chrome. -->
        <Style x:Key="FormFieldTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid x:Name="WindowRootGrid">
    <!-- Static shadow silhouette behind the live content. The window drop shadow is cast here, not as an
         Effect on RootBorder, so animating content inside the window (e.g. the Settings slide) doesn't
         force the whole surface to re-blur every frame. Margin / corner radius / visibility / effect are
         managed in UpdateWindowChrome. -->
    <Border x:Name="WindowShadowBorder" Background="{DynamicResource BgDark}" CornerRadius="7" IsHitTestVisible="False"/>
    <Border x:Name="RootBorder" BorderBrush="{DynamicResource PaneBorder}" BorderThickness="1" Background="{DynamicResource BgDark}" CornerRadius="7">
    <!-- Starts invisible; revealed (fade-in) only after the final shadow/fade positioning pass in
         ContentRendered, so the unpositioned first frame (the "load deform") is never shown. -->
    <Grid x:Name="RootClipGrid" Opacity="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>     <!-- 0: title bar -->
            <RowDefinition Height="Auto"/>   <!-- 1: toolbar -->
            <RowDefinition Height="Auto"/>   <!-- 2: document tab strip (collapses when no PDF is open) -->
            <RowDefinition Height="*"/>      <!-- 3: document content -->
            <RowDefinition Height="28"/>     <!-- 4: footer / status bar -->
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <!-- WindowChrome.CaptionHeight makes this row the draggable caption (native drag + double-click
             maximize), so the explicit MouseLeftButtonDown drag handler is no longer wired. -->
        <Border Grid.Row="0" x:Name="TitleBarBorder" Background="{DynamicResource BgSidebar}" BorderThickness="0"
                CornerRadius="7,7,0,0">
            <Grid>
            <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Wordmark -->
                <Grid Grid.Column="0" VerticalAlignment="Center" Margin="16,0,0,0">
                    <!-- Soft drop shadow: a blurred, offset copy behind the crisp wordmark -->
                    <StackPanel Orientation="Horizontal" Margin="1,2,0,0" Opacity="{DynamicResource IconShadowOpacity}">
                        <StackPanel.Effect><BlurEffect Radius="3"/></StackPanel.Effect>
                        <TextBlock Text="Killer" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontWeight="Bold" FontSize="16" Foreground="#D8000000"/>
                        <TextBlock Text="PDF" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontWeight="Bold" FontSize="16" Foreground="#D8000000"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Killer" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="PDF" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentLogo}"/>
                    </StackPanel>
                </Grid>
                <!-- File name: takes the middle space and truncates before the window buttons -->
                <TextBlock Grid.Column="1" Text="" x:Name="FileNameLabel" VerticalAlignment="Center"
                           Margin="16,0,12,0" TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource TextSecondary}" FontFamily="Consolas" FontSize="11"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="&#xE921;" Click="MinimizeBtn_Click" Style="{StaticResource ChromeButton}" WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="&#xE922;" Click="MaximizeBtn_Click" Style="{StaticResource ChromeButton}" WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <Button Content="&#xE8BB;" Click="CloseBtn_Click" Style="{StaticResource ChromeCloseButton}" WindowChrome.IsHitTestVisibleInChrome="True"/>
                </StackPanel>
            </Grid>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="{DynamicResource BgSidebar}">
            <Grid>
            <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
            <Grid x:Name="ToolbarGrid" SizeChanged="ToolbarGrid_SizeChanged" Margin="4,2">
                <!-- File operations -->
                <StackPanel x:Name="LeftBar" Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- File: New / Open / Close / Save / Print -->
                    <Button Content="&#xE7C3;" Style="{StaticResource ToolbarButton}" Click="New_Click" ToolTip="{DynamicResource Str_TT_NewBlank}"/>
                    <Button x:Name="CloseFileBtn" Content="&#xE711;" Style="{StaticResource ToolbarButton}" Click="CloseFile_Click" ToolTip="{DynamicResource Str_TT_CloseFile}" IsEnabled="False"/>
                    <Button x:Name="OpenFileBtn" Content="&#xE8DA;" Style="{StaticResource ToolbarSplitMain}" Click="Open_Click" ToolTip="{DynamicResource Str_TT_Open}"/>
                    <Button x:Name="OpenRecentBtn" Content="&#xF08E;" FontSize="10" Width="13" Padding="0" Margin="-6,0,0,0" Style="{StaticResource ToolbarButton}" Click="OpenRecent_Click" ToolTip="Recent files"/>
                    <Button x:Name="SaveAsBtn" Content="&#xE74E;" Style="{StaticResource ToolbarSplitMainAccent}" Click="Save_Click" ToolTip="{DynamicResource Str_TT_Save}"/>
                    <Button x:Name="SaveMenuBtn" Content="&#xF08E;" FontSize="10" Width="13" Padding="0" Margin="-6,0,0,0" Style="{StaticResource ToolbarButton}" Click="SaveMenu_Click" ToolTip="Save options"/>
                    <Button Content="&#xE8B9;" Style="{StaticResource ToolbarButton}" Click="SaveFlattened_Click" ToolTip="{DynamicResource Str_TT_SaveFlattened}"/>
                    <Button Content="&#xE749;" Style="{StaticResource ToolbarButton}" Click="Print_Click" ToolTip="{DynamicResource Str_TT_Print}"/>
                    <StackPanel x:Name="GrpPageOps" Orientation="Horizontal">
                        <Rectangle Width="1" Fill="{DynamicResource BorderDim}" Margin="4,6"/>
                        <Button Content="&#xE710;" Style="{StaticResource ToolbarButton}" Click="Merge_Click" ToolTip="{DynamicResource Str_TT_MergePDFs}"/>
                        <Button Content="&#xE8B1;" Style="{StaticResource ToolbarButton}" Click="Split_Click" ToolTip="{DynamicResource Str_TT_ExtractPages}"/>
                    </StackPanel>
                    <StackPanel x:Name="GrpPageEdit" Orientation="Horizontal">
                        <Rectangle Width="1" Fill="{DynamicResource BorderDim}" Margin="4,6"/>
                        <Button Content="&#xE8C6;" Style="{StaticResource ToolbarButton}" Click="Delete_Click" ToolTip="{DynamicResource Str_TT_DeletePages}"/>
                        <Button Content="&#xE74A;" Style="{StaticResource ToolbarButton}" Click="MoveUp_Click" ToolTip="{DynamicResource Str_TT_MovePageUp}"/>
                        <Button Content="&#xE74B;" Style="{StaticResource ToolbarButton}" Click="MoveDown_Click" ToolTip="{DynamicResource Str_TT_MovePageDown}"/>
                    </StackPanel>
                </StackPanel>
                <!-- Edit tools + zoom -->
                <StackPanel x:Name="RightContainer" Orientation="Horizontal" HorizontalAlignment="Right">
                    <StackPanel x:Name="RightBar" Orientation="Horizontal">
                    <Button x:Name="ToolSelectBtn" Content="&#xE8B0;" Style="{StaticResource ToolbarButton}" Click="ToolSelect_Click" ToolTip="{DynamicResource Str_TT_SelectTool}"/>
                    <Button x:Name="ToolTextBtn" Content="&#xE8D2;" Style="{StaticResource ToolbarButton}" Click="ToolText_Click" ToolTip="{DynamicResource Str_TT_TextTool}"/>
                    <Button x:Name="ToolUnderlineBtn" Content="&#xE738;" Style="{StaticResource ToolbarButton}" Click="ToolLine_Click" ToolTip="{DynamicResource Str_TT_LineTool}"/>
                    <Button x:Name="ToolHighlightBtn" Content="&#xE7E6;" Style="{StaticResource ToolbarButton}" Click="ToolHighlight_Click" ToolTip="{DynamicResource Str_TT_HighlightTool}"/>
                    <Button x:Name="ToolDrawBtn" Content="&#xED63;" Style="{StaticResource ToolbarButton}" Click="ToolDraw_Click" ToolTip="{DynamicResource Str_TT_DrawTool}"/>
                    <Button x:Name="ToolImageBtn" Content="&#xEB9F;" Style="{StaticResource ToolbarButton}" Click="ToolImage_Click" ToolTip="{DynamicResource Str_TT_ImageTool}"/>
                    <StackPanel x:Name="GrpSignature" Orientation="Horizontal">
                        <Button x:Name="ToolSignatureBtn" Content="&#xEE56;" Style="{StaticResource ToolbarButton}" Click="ToolSignature_Click" ToolTip="{DynamicResource Str_TT_SignatureTool}"/>
                    </StackPanel>
                    <Button x:Name="ToolCropBtn" Content="&#xE7A8;" Style="{StaticResource ToolbarButton}" Click="ToolCrop_Click" ToolTip="{DynamicResource Str_TT_CropTool}"/>
                    <StackPanel x:Name="GrpUndo" Orientation="Horizontal">
                        <Rectangle Width="1" Fill="{DynamicResource BorderDim}" Margin="4,6"/>
                        <Button Content="&#xE7A7;" Style="{StaticResource ToolbarButton}" Click="Undo_Click" ToolTip="{DynamicResource Str_TT_UndoLast}"/>
                        <Button Content="&#xED62;" Style="{StaticResource ToolbarButtonDanger}" Click="ClearAllAnnotations_Click" ToolTip="{DynamicResource Str_TT_ClearAnnotations}"/>
                        <Rectangle Width="1" Fill="{DynamicResource BorderDim}" Margin="4,6"/>
                        <Button x:Name="SearchBtn" Content="&#xE721;" Style="{StaticResource ToolbarButton}" Click="Search_Click" ToolTip="{DynamicResource Str_TT_Search}"/>
                    </StackPanel>
                    <!-- 10px left gap so the zoom cluster reads as separate from the Search button. -->
                    <Button x:Name="ZoomOutBtn" Content="&#xECC9;" Style="{StaticResource ToolbarButton}"
                            Click="ZoomOut_Click" ToolTip="{DynamicResource Str_TT_ZoomOut}" Margin="10,0,4,0"/>
                    <ComboBox x:Name="ZoomBox" Width="{DynamicResource ZoomBoxWidth}" Height="24" VerticalAlignment="Center"
                              Margin="0,0,2,0"
                              IsEditable="True" IsReadOnly="True"
                              Style="{StaticResource DarkComboBox}"
                              SelectionChanged="ZoomBox_SelectionChanged" ToolTip="{DynamicResource Str_TT_ZoomLevel}">
                        <ComboBoxItem Tag="fitpage" Content="{DynamicResource Str_Zoom_FitPage}"/>
                        <ComboBoxItem Tag="fitwidth" Content="{DynamicResource Str_Zoom_FitWidth}"/>
                        <ComboBoxItem Tag="0.5">50%</ComboBoxItem>
                        <ComboBoxItem Tag="0.75">75%</ComboBoxItem>
                        <ComboBoxItem Tag="1.0" IsSelected="True">100%</ComboBoxItem>
                        <ComboBoxItem Tag="1.25">125%</ComboBoxItem>
                        <ComboBoxItem Tag="1.5">150%</ComboBoxItem>
                        <ComboBoxItem Tag="2.0">200%</ComboBoxItem>
                    </ComboBox>
                    <Button x:Name="ZoomInBtn" Content="&#xECC8;" Style="{StaticResource ToolbarButton}"
                            Click="ZoomIn_Click" ToolTip="{DynamicResource Str_TT_ZoomIn}" Margin="0,0,4,0"/>
                    </StackPanel>
                    <!-- Overflow chevron: shown only when the bar is too narrow -->
                    <ToggleButton x:Name="OverflowChevron" Visibility="Collapsed" Width="26"
                                  VerticalAlignment="Center" Cursor="Hand" Focusable="False"
                                  IsChecked="{Binding IsOpen, ElementName=OverflowPopup, Mode=TwoWay}">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="cb" Background="Transparent" CornerRadius="3" Padding="2">
                                    <TextBlock Text="&#xE712;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                               Foreground="{DynamicResource TextSecondary}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="cb" Property="Background" Value="{DynamicResource BgPanel}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                    <Popup x:Name="OverflowPopup" PlacementTarget="{Binding ElementName=OverflowChevron}"
                           Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                        <Border Background="{DynamicResource BgModal}" BorderBrush="{DynamicResource AccentBorder}"
                                BorderThickness="1" CornerRadius="4" Padding="4" Margin="0,2,6,6">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="14" ShadowDepth="2" Direction="270" Opacity="0.5"/>
                            </Border.Effect>
                            <Grid>
                            <Border CornerRadius="4" IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
                            <StackPanel x:Name="OverflowMenuPanel" ButtonBase.Click="OverflowItem_Click" MinWidth="180">
                                <Button x:Name="MiMerge" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="Merge_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Merge}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiExtract" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="Split_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE8B1;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Extract}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiDelete" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="Delete_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE8C6;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Delete}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiMoveUp" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="MoveUp_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE74A;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_MoveUp}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiMoveDown" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="MoveDown_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE74B;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_MoveDown}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiText" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolText_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE8D2;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Text}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiUnderline" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolLine_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE738;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Line}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiHighlight" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolHighlight_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE7E6;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Highlight}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiDraw" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolDraw_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xED63;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Draw}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiImage" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolImage_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xEB9F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Image}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiSignature" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolSignature_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xEE56;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Signature}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiCrop" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ToolCrop_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE7A8;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Crop}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiUndo" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="Undo_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE7A7;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Undo}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                                <Button x:Name="MiClear" Style="{StaticResource OverflowMenuButton}" Visibility="Collapsed" Click="ClearAllAnnotations_Click">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xED62;" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="24" Foreground="{DynamicResource TextPrimary}"/><TextBlock Text="{DynamicResource Str_Lbl_Clear}" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                            </StackPanel>
                            </Grid>
                        </Border>
                    </Popup>
                </StackPanel>
            </Grid>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="2" Grid.RowSpan="2" x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <!-- The sidebar lives in column 0 (left, default) or column 2 (right). ApplySidebarSide
                     swaps which column is the sized sidebar (24-260px) and which is the star document
                     area, and repoints _sidebarCol accordingly. SidebarCol never collapses to 0 - it
                     shrinks to 24px (the toggle strip width). -->
                <ColumnDefinition x:Name="SidebarCol" Width="180" MinWidth="24" MaxWidth="234"/>
                <!-- Dedicated drag handle column — keeps the splitter independent of the sidebar content -->
                <ColumnDefinition Width="6"/>
                <ColumnDefinition x:Name="DocCol" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   <!-- row 0: tab strip (above the document only) -->
                <RowDefinition Height="*"/>      <!-- row 1: sidebar / splitter / document -->
            </Grid.RowDefinitions>

            <!-- Sidebar column: inner grid splits content from permanent toggle strip. The toggle
                 strip is kept on the side facing the document (right when the sidebar is on the
                 left, left when the sidebar is on the right) by ApplySidebarSide. -->
            <Grid x:Name="SidebarOuterGrid" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="SbContentCol" Width="*"/>   <!-- page list content -->
                    <ColumnDefinition x:Name="SbToggleCol" Width="24"/>   <!-- toggle strip — always visible -->
                </Grid.ColumnDefinitions>

                <!-- Page list content (hidden when collapsed) -->
                <Border x:Name="SidebarBorder" Grid.Column="0"
                        Background="{DynamicResource BgSidebar}"
                        BorderBrush="{DynamicResource BorderDim}" BorderThickness="0,0,0,0">
                    <Grid>
                    <!-- Film grain - sits on the sidebar background, behind the thumbnails -->
                    <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}">
                        <Border.Background>
                            <ImageBrush x:Name="SidebarGrainBrush"
                                        TileMode="Tile"
                                        ViewportUnits="Absolute"
                                        Viewport="0,0,256,256"
                                        Stretch="None"/>
                        </Border.Background>
                    </Border>
                    <DockPanel x:Name="SidebarContentPanel">
                        <!-- Sticky header — lives outside the ScrollViewer so it never scrolls away -->
                        <!-- Tab row: PAGES (left) | OUTLINES (right) -->
                        <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="10,6,8,0">
                            <Button x:Name="SidebarPagesTab" Content="{DynamicResource Str_TabPages}"
                                    DockPanel.Dock="Left"
                                    Style="{StaticResource SidebarTabBtn}"
                                    Foreground="{DynamicResource Accent}"
                                    Click="SidebarPagesTab_Click"/>
                            <Button x:Name="SidebarOutlinesTab" Content="{DynamicResource Str_TabOutlines}"
                                    DockPanel.Dock="Right"
                                    Style="{StaticResource SidebarTabBtn}"
                                    IsEnabled="False"
                                    Click="SidebarOutlinesTab_Click"/>
                        </DockPanel>
                        <!-- Page controls row (hidden in outlines mode) -->
                        <DockPanel x:Name="PageControlsRow" DockPanel.Dock="Top" LastChildFill="False" Margin="12,2,8,4">
                            <TextBox x:Name="PageJumpBox" DockPanel.Dock="Left"
                                     Width="30" Height="20" VerticalAlignment="Center"
                                     TextAlignment="Center" VerticalContentAlignment="Center"
                                     FontFamily="Consolas" FontSize="11"
                                     Background="{DynamicResource BgPanel}"
                                     Foreground="{DynamicResource TextPrimary}"
                                     BorderBrush="{DynamicResource BorderDim}" BorderThickness="1"
                                     CaretBrush="{DynamicResource Accent}"
                                     SelectionBrush="{DynamicResource AccentDim}"
                                     IsEnabled="False"
                                     FocusVisualStyle="{x:Null}"
                                     KeyDown="PageJumpBox_KeyDown"
                                     GotFocus="PageJumpBox_GotFocus"
                                     ToolTip="{DynamicResource Str_TT_GoToPage}"
                                     Margin="0,0,2,0">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderDim}"/>
                                            <!-- Custom template so the default WPF TextBox focus/hover
                                                 border (light blue) doesn't override the themed border. -->
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="TextBox">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="2">
                                                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            <TextBlock x:Name="PageTotalLabel" DockPanel.Dock="Left"
                                       Text="/ –" VerticalAlignment="Center"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{DynamicResource TextSecondary}"
                                       Margin="2,0,0,0"/>
                        </DockPanel><!-- /PageControlsRow -->
                        <!-- Scrollable page list (virtualized) -->
                        <ListBox x:Name="PageList" Style="{StaticResource PageListBox}"
                                 BorderThickness="0" SelectionMode="Extended"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 VirtualizingPanel.ScrollUnit="Pixel"
                                 ScrollViewer.CanContentScroll="True"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 SelectionChanged="PageList_SelectionChanged"
                                 AllowDrop="True" Drop="PageList_Drop" DragOver="PageList_DragOver"
                                 PreviewMouseLeftButtonDown="PageList_PreviewMouseLeftButtonDown"
                                 PreviewMouseMove="PageList_PreviewMouseMove"
                                 MouseRightButtonUp="PageList_MouseRightButtonUp">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:PageThumbnailVm}">
                                    <!-- Thumbnail scales to the sidebar width (the item stretches) with side
                                         margins that clear the scrollbar, so it never gets clipped. -->
                                    <StackPanel Margin="8,2,8,2">
                                        <Border x:Name="ThumbBorder"
                                                Background="White"
                                                BorderBrush="{DynamicResource BgDark}"
                                                BorderThickness="1"
                                                MaxWidth="200"
                                                HorizontalAlignment="Center">
                                            <Image Source="{Binding Thumbnail}"
                                                   Stretch="Uniform"/>
                                        </Border>
                                        <TextBlock Text="{Binding Label}"
                                                   FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"
                                                   FontSize="10"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,2,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                                    <Style.Triggers>
                                                        <!-- Selected page label uses the theme's selection foreground. -->
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                            <Setter TargetName="ThumbBorder" Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <!-- Outline/bookmark tree (visible when OUTLINES tab is active) -->
                        <ScrollViewer x:Name="OutlineScrollViewer"
                                      Visibility="Collapsed"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      PreviewMouseWheel="OutlineScroll_PreviewMouseWheel">
                            <TreeView x:Name="OutlineTree"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Style="{StaticResource OutlineTreeStyle}"
                                      ItemContainerStyle="{StaticResource OutlineItemStyle}"
                                      Padding="4,2"/>
                        </ScrollViewer>
                    </DockPanel>
                    </Grid>
                </Border>

                <!-- Toggle strip — 24px wide vertical icon bar -->
                <Border x:Name="SidebarToggleStrip" Grid.Column="1"
                        Background="{DynamicResource BgSidebar}"
                        BorderThickness="0">
                    <Grid>
                        <!-- Film grain - matches the sidebar so the strip blends in -->
                        <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}">
                            <Border.Background>
                                <ImageBrush x:Name="ToggleGrainBrush"
                                            TileMode="Tile"
                                            ViewportUnits="Absolute"
                                            Viewport="0,0,256,256"
                                            Stretch="None"/>
                            </Border.Background>
                        </Border>
                        <!-- Collapse/expand arrow at top -->
                        <Button x:Name="SidebarToggleBtn"
                                Content="&#xE76B;" ToolTip="{DynamicResource Str_TT_CollapseSidebar}"
                                Style="{StaticResource ToolbarButton}"
                                Width="24" Padding="0" FontSize="10"
                                VerticalAlignment="Top" Margin="0,6,0,0"
                                Click="SidebarToggle_Click"/>
                        <!-- Settings and help icons pinned to bottom -->
                        <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,6">
                            <Button Click="ShortcutHelp_Click"
                                    Cursor="Hand" FocusVisualStyle="{x:Null}"
                                    ToolTip="{DynamicResource Str_TT_KeyboardShortcuts}"
                                    Width="24" Height="24" Padding="0" Margin="0,0,0,6">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <TextBlock Text="?"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       FontFamily="Consolas" FontSize="18"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                                </TextBlock.Effect>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                                                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button x:Name="SettingsBtn"
                                    Click="SettingsBtn_Click"
                                    Cursor="Hand" FocusVisualStyle="{x:Null}"
                                    ToolTip="{DynamicResource Str_TT_Settings}"
                                    Width="24" Height="24" Padding="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <TextBlock Text="&#xE713;"
                                                       FontFamily="Segoe MDL2 Assets"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       FontSize="16"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="270" Opacity="{DynamicResource IconShadowOpacity}"/>
                                                </TextBlock.Effect>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                                                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Drag-to-resize splitter (only active in OUTLINES mode) -->
            <GridSplitter x:Name="SidebarSplitter"
                          Grid.Column="1"
                          Grid.Row="1"
                          Width="6"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource BgCanvas}"
                          Cursor="SizeWE"
                          IsEnabled="True"
                          BorderThickness="0,0,1,0"
                          ResizeBehavior="PreviousAndNext"
                          ResizeDirection="Columns">
                <GridSplitter.Template>
                    <ControlTemplate TargetType="GridSplitter">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource DragLine}" BorderThickness="{TemplateBinding BorderThickness}">
                            <!-- grip dots, centred vertically -->
                            <ItemsControl VerticalAlignment="Center" HorizontalAlignment="Center"
                                          IsHitTestVisible="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <Ellipse Width="3" Height="3" Margin="0,2" Fill="{DynamicResource BgDragHandle}"/>
                                <Ellipse Width="3" Height="3" Margin="0,2" Fill="{DynamicResource BgDragHandle}"/>
                                <Ellipse Width="3" Height="3" Margin="0,2" Fill="{DynamicResource BgDragHandle}"/>
                                <Ellipse Width="3" Height="3" Margin="0,2" Fill="{DynamicResource BgDragHandle}"/>
                                <Ellipse Width="3" Height="3" Margin="0,2" Fill="{DynamicResource BgDragHandle}"/>
                            </ItemsControl>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BgCanvas}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BgCanvas}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>

            <!-- Elevation shadow cast by the document onto the sidebar: pokes out of the splitter
                 handle (the 3-dot drag handle) onto the page list. ApplySidebarSide sets its side and
                 flips its direction. Black-alpha (reads on light/gray sidebars, subtle on near-black). -->
            <Border x:Name="SidebarShadow" Grid.Column="1" Grid.Row="1" IsHitTestVisible="False" Width="12"
                    HorizontalAlignment="Left" Margin="-12,0,0,0" Panel.ZIndex="50"
                    RenderTransformOrigin="0.5,0.5">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Offset="0.0" Color="#3A000000"/>
                        <GradientStop Offset="1.0" Color="#00000000"/>
                    </LinearGradientBrush>
                </Border.Background>
                <!-- Feather the top and bottom so the shadow has no hard horizontal ends. -->
                <Border.OpacityMask>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.00" Color="#00000000"/>
                        <GradientStop Offset="0.06" Color="#FF000000"/>
                        <GradientStop Offset="0.94" Color="#FF000000"/>
                        <GradientStop Offset="1.00" Color="#00000000"/>
                    </LinearGradientBrush>
                </Border.OpacityMask>
            </Border>

            <!-- Tab strip = the grainy "menubar" band for row 0. Its background + grain + fade span the
                 splitter column AND the document column, so the band is one continuous grainy strip
                 with no unpainted gap (the old "6x31 rectangle" was root background showing through the
                 bare splitter column). The tabs sit on top, offset by TabScroll's margin to start at the
                 document edge. ApplySidebarSide sets the start column / span / tab offset per side. -->
            <Border Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" x:Name="TabStripBorder"
                    Background="{DynamicResource BgSidebar}" Visibility="Collapsed">
                    <Grid>
                        <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
                        <Border x:Name="TabStripFade" IsHitTestVisible="False" Background="{DynamicResource TabStripFadeBrush}"/>
                        <ScrollViewer x:Name="TabScroll" Margin="6,0,0,0" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden">
                            <StackPanel x:Name="TabStrip" Orientation="Horizontal" Margin="1,0,6,0"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            <!-- 1px PaneBorder cap across the TOP of the splitter column so the document's top border reads
                 as one continuous line into the corner; the splitter's vertical DragLine otherwise pokes
                 up past the pane's top border at that corner. High ZIndex so it paints over the splitter. -->
            <Border Grid.Row="1" Grid.Column="1" x:Name="SplitterTopCap" VerticalAlignment="Top" Height="1"
                    Background="{DynamicResource PaneBorder}" Panel.ZIndex="60" IsHitTestVisible="False"/>
            <!-- Document pane: row 1 of the document column. -->
            <Border Grid.Row="1" Grid.Column="2" x:Name="DocPaneBorder" Background="{DynamicResource BgCanvas}"
                    BorderBrush="{DynamicResource PaneBorder}" BorderThickness="1">
                <Grid>
                    <!-- Film grain - sits on the canvas background, behind the document -->
                    <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}">
                        <Border.Background>
                            <ImageBrush x:Name="GrainBrush"
                                        TileMode="Tile"
                                        ViewportUnits="Absolute"
                                        Viewport="0,0,256,256"
                                        Stretch="None"/>
                        </Border.Background>
                    </Border>

                    <!-- Drop zone -->
                    <Border x:Name="DropZone" Background="Transparent"
                            AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver"
                            MouseLeftButtonDown="DropZone_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Drop target, centered in the remaining space -->
                            <Border Grid.Column="0" BorderBrush="{DynamicResource DropBorder}" BorderThickness="2"
                                    CornerRadius="8" Padding="40"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Background="Transparent" Style="{x:Null}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Drop PDF here" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="20"
                                               Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="or click to browse" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="13"
                                               Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Recent files sidebar (populated in code-behind; hidden when empty) -->
                            <Border x:Name="RecentFilesBox" Grid.Column="1" Width="280" Visibility="Collapsed"
                                    Background="{DynamicResource BgRecentPanel}"
                                    BorderBrush="{DynamicResource PaneBorder}" BorderThickness="1,0,0,0">
                                <Grid>
                                    <Border Background="{DynamicResource GrainBrushShared}" IsHitTestVisible="False"
                                            Opacity="{DynamicResource GrainOpacity}"/>
                                    <DockPanel Margin="18,22,12,16">
                                        <TextBlock DockPanel.Dock="Top" Text="RECENT" FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"
                                                   FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource Accent}" Margin="2,0,0,10"/>
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                            <ItemsControl x:Name="RecentFilesList"/>
                                        </ScrollViewer>
                                    </DockPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- PDF page with annotation overlay -->
                    <ScrollViewer x:Name="PagePreviewPanel" Visibility="Collapsed"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  Background="Transparent"
                                  PreviewMouseWheel="PagePreview_PreviewMouseWheel"
                                  SizeChanged="PagePreviewPanel_SizeChanged"
                                  PreviewMouseDown="PagePreviewPanel_PreviewMouseDown"
                                  PreviewMouseMove="PagePreviewPanel_PreviewMouseMove"
                                  PreviewMouseUp="PagePreviewPanel_PreviewMouseUp"
                                  MouseRightButtonUp="DocPaneBackground_RightClick"
                                  AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
                        <Border VerticalAlignment="Top" HorizontalAlignment="Center" Padding="12">
                            <!-- Override the window's Display/ClearType text mode for the zoomed
                                 document surface. Display + ClearType pixel-snaps and color-fringes
                                 when scaled, giving hard-edged text on annotations and form fields;
                                 Ideal + Grayscale stays smoothly anti-aliased at any zoom, matching
                                 how other PDF editors render. -->
                            <Grid x:Name="PageContentGrid"
                                  TextOptions.TextFormattingMode="Ideal"
                                  TextOptions.TextRenderingMode="Grayscale">
                                <Grid.LayoutTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Grid.LayoutTransform>
                                <WrapPanel x:Name="PageContentPanel" Orientation="Horizontal">
                                    <!-- Primary page (always present) -->
                                    <Border Background="White" VerticalAlignment="Top" Margin="0,0,12,12">
                                        <Grid>
                                            <Image x:Name="PageImage" RenderOptions.BitmapScalingMode="HighQuality" Stretch="None"/>
                                            <Canvas x:Name="AnnotationCanvas" Background="Transparent" ClipToBounds="True"
                                                    PreviewMouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                                                    MouseMove="Canvas_MouseMove"
                                                    MouseLeave="Canvas_MouseLeave"
                                                    PreviewMouseLeftButtonUp="Canvas_MouseLeftButtonUp"/>
                                        </Grid>
                                    </Border>
                                    <!-- Additional pages rendered dynamically in code-behind -->
                                </WrapPanel>
                                <!-- Continuous scroll panel - shown in Continuous view mode -->
                                <StackPanel x:Name="ContinuousPanel"
                                            HorizontalAlignment="Center"
                                            Orientation="Vertical"
                                            Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Border lines bridging the slider handle to the toolbar/footer (the doc-pane border
                 doesn't cover the splitter column). They span the splitter + document columns;
                 ApplySidebarSide sets the start column for the sidebar side. Top = doc-pane border
                 color; bottom = slider accent, which continues into the sidebar-facing stripe. -->
            <Border x:Name="DocTopAccent" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"
                    Height="1" VerticalAlignment="Top" IsHitTestVisible="False" Panel.ZIndex="60"
                    Background="{DynamicResource PaneBorder}"/>
            <Border x:Name="DocBottomAccent" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"
                    Height="1" VerticalAlignment="Bottom" IsHitTestVisible="False" Panel.ZIndex="60"
                    Background="{DynamicResource DragLine}"/>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="4" x:Name="FooterBorder" Background="{DynamicResource BgSidebar}" CornerRadius="0,0,7,7">
            <Grid>
            <Border IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
            <!-- Mirror of the tab-strip shadow gradient, flipped so it darkens at the TOP (toward the
                 document) and fades down. UpdateTabStripFade clips it to the document column too. -->
            <Border x:Name="FooterFade" IsHitTestVisible="False" Background="{DynamicResource TabStripFadeBrush}"
                    VerticalAlignment="Top" Height="12"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleY="-1"/>
                </Border.RenderTransform>
            </Border>
            <Grid Margin="16,0">
                <TextBlock x:Name="StatusText" Text="{DynamicResource Str_Ready}"
                           Foreground="{DynamicResource TextSecondary}"
                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" VerticalAlignment="Center"/>
                <!-- Portable mode indicator (hidden when installed) -->
                <StackPanel x:Name="PortableBadge" Orientation="Horizontal"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="{DynamicResource Str_Portable}" Foreground="{DynamicResource TextSecondary}"
                               FontFamily="Consolas" FontSize="9"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button x:Name="InstallBtn" Content="{DynamicResource Str_InstallBtn}"
                            Click="Install_Click"
                            FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold"
                            Foreground="White"
                            Background="{DynamicResource InstallBtnBg}"
                            BorderThickness="0"
                            Padding="12,2" Cursor="Hand"
                            VerticalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <!-- Separate shadow element keeps the button label crisp -->
                                    <Border Background="{TemplateBinding Background}" CornerRadius="3" IsHitTestVisible="False">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.45"/>
                                        </Border.Effect>
                                    </Border>
                                    <Border x:Name="ib" Background="{TemplateBinding Background}"
                                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ib" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="VersionLabel" Text="v1.5.7"
                               Foreground="{DynamicResource TextFooter}" FontFamily="Consolas"
                               FontSize="10" VerticalAlignment="Center" Margin="0,0,12,0"
                               Cursor="Hand" ToolTip="{DynamicResource Str_TT_About}"
                               MouseLeftButtonDown="VersionLabel_MouseLeftButtonDown"/>
                    <TextBlock FontSize="10" VerticalAlignment="Center"
                               FontFamily="Consolas">
                        <Run Text="&#x00A9; 2026 " Foreground="{DynamicResource TextFooter}"/>
                        <Hyperlink NavigateUri="https://thekiller.net"
                                   RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="{DynamicResource TextFooter}" TextDecorations="None">
                            <Run Text="Steve the Killer"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
            </Grid>
        </Border>

        <!-- Bottom-right resize grip. The WPF CanResizeWithGrip dots land out in the transparent shadow
             margin, so this custom one sits at the visible content corner and forwards a HTBOTTOMRIGHT
             resize to Windows on drag. -->
        <Canvas x:Name="ResizeGripDots" Grid.Row="4" Width="13" Height="13"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,4,4"
                Background="Transparent" Cursor="SizeNWSE"
                MouseLeftButtonDown="ResizeGrip_MouseDown">
            <Rectangle Canvas.Left="10" Canvas.Top="2"  Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
            <Rectangle Canvas.Left="6"  Canvas.Top="6"  Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
            <Rectangle Canvas.Left="10" Canvas.Top="6"  Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
            <Rectangle Canvas.Left="2"  Canvas.Top="10" Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
            <Rectangle Canvas.Left="6"  Canvas.Top="10" Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
            <Rectangle Canvas.Left="10" Canvas.Top="10" Width="2" Height="2" Fill="{DynamicResource TextDim}"/>
        </Canvas>

        <!-- Settings overlay. No Background, so it doesn't catch clicks - the panel is non-modal and the
             window stays interactive while it's open (a click elsewhere is dismissed by a window-level
             handler that lets the click through). -->
        <Grid x:Name="SettingsOverlay"
              Grid.RowSpan="5"
              Panel.ZIndex="9999"
              Visibility="Collapsed">
            <Grid x:Name="SettingsPanel" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="186,0,0,28" Width="228"
                  MaxHeight="{Binding ActualHeight, ElementName=DocPaneBorder}">
                <!-- Left edge stays flush against the sidebar's right edge; the panel expands rightward
                     out of the sidebar (its Width animates) and shrinks back into it on close. The inner
                     borders keep a fixed width so the content doesn't reflow while the Grid is clipped. -->
                <!-- Opaque body + shadow, using the same BgPanel + film grain as the annotate bar. No
                     left border / left corners - that edge sits against the sidebar. -->
                <Border x:Name="SettingsCardShadow" Background="{DynamicResource BgFlyout}" CornerRadius="0,6,6,0" IsHitTestVisible="False"
                        Width="228" HorizontalAlignment="Left">
                    <Border.Effect>
                        <!-- Offset right (Direction 0) with depth >= blur so the shadow falls to the
                             right/below and never bleeds left over the sidebar. ApplySidebarSide flips
                             Direction to 180 when the sidebar (and panel) move to the right. -->
                        <DropShadowEffect Color="Black" BlurRadius="14" ShadowDepth="14" Direction="0" Opacity="0.5"/>
                    </Border.Effect>
                </Border>
                <Border x:Name="SettingsCardGrain" Background="{DynamicResource GrainBrushShared}" Opacity="0.035"
                        CornerRadius="0,6,6,0" Width="228" HorizontalAlignment="Left" IsHitTestVisible="False"/>
                <Border x:Name="SettingsCardBorder" Background="Transparent"
                        BorderBrush="{DynamicResource AccentBorder}"
                        BorderThickness="0,1,1,1"
                        CornerRadius="0,6,6,0"
                        Width="228" HorizontalAlignment="Left"
                        Padding="20,16,6,20"
                        MouseLeftButtonDown="SettingsOverlayCard_MouseLeftButtonDown"
                        MouseRightButtonDown="SettingsOverlayCard_MouseLeftButtonDown">
                <Grid>
                <Border CornerRadius="6" Margin="-20,-16,-6,-20" IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
                <!-- Bleed the scroll viewer's left edge outward (negative margin) so its clip region
                     starts ~12px left of the content. The content is indented with a StackPanel margin
                     instead of ScrollViewer padding (ScrollViewer padding clips its content), so the row
                     highlights can extend left of the words with their rounded corner intact. -->
                <ScrollViewer x:Name="SettingsScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                              Margin="-12,0,0,0">
                <StackPanel Margin="12,0,0,0">
                    <Grid x:Name="SettingsHeader" Background="Transparent" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Str_Settings}"
                                   FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="14" FontWeight="SemiBold"
                                   Foreground="{DynamicResource HeaderAccent}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Direction="270" Opacity="0.55"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <Button Click="SettingsOverlayClose_Click"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                Width="24" Height="24" Content="&#xE711;"
                                Style="{StaticResource DangerCloseButton}"/>
                    </Grid>
                    <TextBlock Text="{DynamicResource Str_Language}" Style="{StaticResource SettingsSubheader}"
                               Margin="0,0,0,8"/>
                    <!-- Language picker: a row that opens a flyout submenu. Languages are
                         listed by their own native name (autonym) so the list scales to any
                         number of languages without translating each name into every locale. -->
                    <Grid Margin="0,0,0,4">
                        <ToggleButton x:Name="LangMenuButton"
                                      Checked="SettingsMenu_Toggled" Unchecked="SettingsMenu_Toggled"
                                      Cursor="Hand" FocusVisualStyle="{x:Null}"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="RowBd" Background="Transparent" Margin="-10,0,2,0" Padding="10,4,10,4" CornerRadius="3">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource SettingsOpenRowBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right"
                                           FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{DynamicResource TextSecondary}">
                                    <!-- Up chevron when the section is collapsed, down when expanded. -->
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xE70E;"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Text" Value="&#xE70D;"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock x:Name="LangCurrentLabel" Text="English" Style="{StaticResource SettingsAccentValue}"/>
                            </DockPanel>
                        </ToggleButton>
                        </Grid>
                        <!-- Language options expand inline below the row (accordion). -->
                        <StackPanel x:Name="LangSubmenu" Visibility="Collapsed" Margin="10,2,0,6">
                                    <RadioButton x:Name="LangEnRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangEnRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="English" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="en-US" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangBnRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangBnRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="বাংলা" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="bn" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangDeRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangDeRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Deutsch" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="de-DE" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangEsRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangEsRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Español" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="es" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangTrRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangTrRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Türkçe" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="tr-TR" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangZhCNRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangZhCNRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="中文 (简体)" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="zh-CN" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                                    <RadioButton x:Name="LangZhTWRadio" GroupName="LangGroup" Style="{StaticResource ThemeRadio}"
                                                 Checked="LangZhTWRadio_Checked">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="中文 (繁體)" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI"/>
                                            <TextBlock Grid.Column="1" Text="zh-TW" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextSecondary}"/>
                                        </Grid>
                                    </RadioButton>
                        </StackPanel>
                    <TextBlock Text="{DynamicResource Str_Theme}" Style="{StaticResource SettingsSubheader}"
                               Margin="0,12,0,8"/>
                    <!-- Theme picker: a row that opens a flyout submenu, mirroring the Language picker
                         so the Settings panel stays compact as more themes are added. -->
                    <Grid Margin="0,0,0,4">
                        <ToggleButton x:Name="ThemeMenuButton"
                                      Checked="SettingsMenu_Toggled" Unchecked="SettingsMenu_Toggled"
                                      Cursor="Hand" FocusVisualStyle="{x:Null}"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="RowBd" Background="Transparent" Margin="-10,0,2,0" Padding="10,4,10,4" CornerRadius="3">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource SettingsOpenRowBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right"
                                           FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{DynamicResource TextSecondary}">
                                    <!-- Up chevron when the section is collapsed, down when expanded. -->
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xE70E;"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Text" Value="&#xE70D;"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock x:Name="ThemeCurrentLabel" Text="Dark" Style="{StaticResource SettingsAccentValue}"/>
                            </DockPanel>
                        </ToggleButton>
                        </Grid>
                        <StackPanel x:Name="ThemeSubmenu" Visibility="Collapsed" Margin="10,2,0,6">
                                    <RadioButton x:Name="ThemeDarkRadio"
                                                 Content="{DynamicResource Str_Theme_Dark}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeDarkRadio_Checked"/>
                                    <!-- Each family's picker sits under its own theme. The rows animate their HEIGHT
                                         (clipped), so when you switch, the old row shrinks while the new one grows by
                                         the same amount: the picker slides to the selected theme and the total menu
                                         height never changes. ROYGBIV order. -->
                                    <StackPanel x:Name="DarkAccentRow" Orientation="Horizontal" Margin="10,-5,0,5" Height="0" ClipToBounds="True" Visibility="Collapsed">
                                        <Border x:Name="AccentDotRed"    Style="{StaticResource AccentDot}" Background="#DD504B" Tag="Red"    MouseLeftButtonUp="AccentDot_Click"/>
                                        <Border x:Name="AccentDotOrange" Style="{StaticResource AccentDot}" Background="#E8962C" Tag="Orange" MouseLeftButtonUp="AccentDot_Click"/>
                                        <Border x:Name="AccentDotGreen"  Style="{StaticResource AccentDot}" Background="#1EA54C" Tag="Green"  MouseLeftButtonUp="AccentDot_Click"/>
                                        <Border x:Name="AccentDotTeal"   Style="{StaticResource AccentDot}" Background="#1FB8A8" Tag="Teal"   MouseLeftButtonUp="AccentDot_Click"/>
                                        <Border x:Name="AccentDotBlue"   Style="{StaticResource AccentDot}" Background="#4580D9" Tag="Blue"   MouseLeftButtonUp="AccentDot_Click"/>
                                        <Border x:Name="AccentDotPurple" Style="{StaticResource AccentDot}" Background="#B982E3" Tag="Purple" MouseLeftButtonUp="AccentDot_Click"/>
                                    </StackPanel>
                                    <RadioButton x:Name="ThemeLightRadio"
                                                 Content="{DynamicResource Str_Theme_Light}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeLightRadio_Checked"/>
                                    <StackPanel x:Name="LightAccentRow" Orientation="Horizontal" Margin="10,-5,0,5" Height="0" ClipToBounds="True" Visibility="Collapsed">
                                        <Border x:Name="AccentDotLightRed"    Style="{StaticResource AccentDot}" Background="#931A1A" Tag="Red"    MouseLeftButtonUp="AccentDotLight_Click"/>
                                        <Border x:Name="AccentDotLightOrange" Style="{StaticResource AccentDot}" Background="#C7710F" Tag="Orange" MouseLeftButtonUp="AccentDotLight_Click"/>
                                        <Border x:Name="AccentDotLightGreen"  Style="{StaticResource AccentDot}" Background="#1B5E20" Tag="Green"  MouseLeftButtonUp="AccentDotLight_Click"/>
                                        <Border x:Name="AccentDotLightTeal"   Style="{StaticResource AccentDot}" Background="#0D827E" Tag="Teal"   MouseLeftButtonUp="AccentDotLight_Click"/>
                                        <Border x:Name="AccentDotLightBlue"   Style="{StaticResource AccentDot}" Background="#18608E" Tag="Blue"   MouseLeftButtonUp="AccentDotLight_Click"/>
                                        <Border x:Name="AccentDotLightPurple" Style="{StaticResource AccentDot}" Background="#5A1690" Tag="Purple" MouseLeftButtonUp="AccentDotLight_Click"/>
                                    </StackPanel>
                                    <RadioButton x:Name="ThemeHCRadio"
                                                 Content="{DynamicResource Str_Theme_Black}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeHCRadio_Checked"/>
                                    <StackPanel x:Name="BlackAccentRow" Orientation="Horizontal" Margin="10,-5,0,5" Height="0" ClipToBounds="True" Visibility="Collapsed">
                                        <Border x:Name="AccentDotBlackRed"    Style="{StaticResource AccentDot}" Background="#FF2929" Tag="Red"    MouseLeftButtonUp="AccentDotBlack_Click"/>
                                        <Border x:Name="AccentDotBlackOrange" Style="{StaticResource AccentDot}" Background="#FF910A" Tag="Orange" MouseLeftButtonUp="AccentDotBlack_Click"/>
                                        <Border x:Name="AccentDotBlackGreen"  Style="{StaticResource AccentDot}" Background="#00FF66" Tag="Green"  MouseLeftButtonUp="AccentDotBlack_Click"/>
                                        <Border x:Name="AccentDotBlackTeal"   Style="{StaticResource AccentDot}" Background="#0AFFE7" Tag="Teal"   MouseLeftButtonUp="AccentDotBlack_Click"/>
                                        <Border x:Name="AccentDotBlackBlue"   Style="{StaticResource AccentDot}" Background="#298DFF" Tag="Blue"   MouseLeftButtonUp="AccentDotBlack_Click"/>
                                        <Border x:Name="AccentDotBlackPurple" Style="{StaticResource AccentDot}" Background="#B829FF" Tag="Purple" MouseLeftButtonUp="AccentDotBlack_Click"/>
                                    </StackPanel>
                                    <RadioButton x:Name="ThemeBloodRadio"
                                                 Content="{DynamicResource Str_Theme_Blood}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeBloodRadio_Checked"/>
                                    <RadioButton x:Name="ThemeGreedRadio"
                                                 Content="{DynamicResource Str_Theme_Greed}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeGreedRadio_Checked"/>
                                    <RadioButton x:Name="ThemeCyanoticRadio"
                                                 Content="{DynamicResource Str_Theme_Cyanotic}"
                                                 GroupName="ThemeGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ThemeCyanoticRadio_Checked"/>
                        </StackPanel>
                    <TextBlock Text="{DynamicResource Str_Toolbar}" Style="{StaticResource SettingsSubheader}"
                               Margin="0,12,0,8"/>
                    <!-- Toolbar style picker: a flyout submenu mirroring Theme/Language. -->
                    <Grid Margin="0,0,0,4">
                        <ToggleButton x:Name="ToolbarMenuButton"
                                      Checked="SettingsMenu_Toggled" Unchecked="SettingsMenu_Toggled"
                                      Cursor="Hand" FocusVisualStyle="{x:Null}"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="RowBd" Background="Transparent" Margin="-10,0,2,0" Padding="10,4,10,4" CornerRadius="3">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource SettingsOpenRowBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right"
                                           FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{DynamicResource TextSecondary}">
                                    <!-- Up chevron when the section is collapsed, down when expanded. -->
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xE70E;"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Text" Value="&#xE70D;"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock x:Name="ToolbarCurrentLabel" Text="Text beside icons" Style="{StaticResource SettingsAccentValue}"/>
                            </DockPanel>
                        </ToggleButton>
                        </Grid>
                        <StackPanel x:Name="ToolbarSubmenu" Visibility="Collapsed" Margin="10,2,0,6">
                                    <RadioButton x:Name="ToolbarSmallRadio"
                                                 Content="{DynamicResource Str_Toolbar_SmallIcons}"
                                                 GroupName="ToolbarGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ToolbarSmallRadio_Checked"/>
                                    <RadioButton x:Name="ToolbarLargeRadio"
                                                 Content="{DynamicResource Str_Toolbar_LargeIcons}"
                                                 GroupName="ToolbarGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ToolbarLargeRadio_Checked"/>
                                    <RadioButton x:Name="ToolbarBesideRadio"
                                                 Content="{DynamicResource Str_Toolbar_TextBeside}"
                                                 GroupName="ToolbarGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ToolbarBesideRadio_Checked"/>
                                    <RadioButton x:Name="ToolbarUnderRadio"
                                                 Content="{DynamicResource Str_Toolbar_TextUnder}"
                                                 GroupName="ToolbarGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ToolbarUnderRadio_Checked"/>
                                    <RadioButton x:Name="ToolbarOnlyRadio"
                                                 Content="{DynamicResource Str_Toolbar_TextOnly}"
                                                 GroupName="ToolbarGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ToolbarOnlyRadio_Checked"/>
                        </StackPanel>
                    <!-- Sidebar position picker (Left / Right) -->
                    <TextBlock Text="{DynamicResource Str_Sidebar}" Style="{StaticResource SettingsSubheader}"
                               Margin="0,12,0,8"/>
                    <Grid Margin="0,0,0,4">
                        <ToggleButton x:Name="SidebarMenuButton"
                                      Checked="SettingsMenu_Toggled" Unchecked="SettingsMenu_Toggled"
                                      Cursor="Hand" FocusVisualStyle="{x:Null}"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="RowBd" Background="Transparent" Margin="-10,0,2,0" Padding="10,4,10,4" CornerRadius="3">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource SettingsOpenRowBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right"
                                           FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{DynamicResource TextSecondary}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xE70E;"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Text" Value="&#xE70D;"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock x:Name="SidebarCurrentLabel" Text="Left" Style="{StaticResource SettingsAccentValue}"/>
                            </DockPanel>
                        </ToggleButton>
                    </Grid>
                    <StackPanel x:Name="SidebarSubmenu" Visibility="Collapsed" Margin="10,2,0,6">
                        <RadioButton x:Name="SidebarLeftRadio" Content="{DynamicResource Str_Sidebar_Left}"
                                     GroupName="SidebarGroup" Style="{StaticResource ThemeRadio}"
                                     Checked="SidebarLeftRadio_Checked"/>
                        <RadioButton x:Name="SidebarRightRadio" Content="{DynamicResource Str_Sidebar_Right}"
                                     GroupName="SidebarGroup" Style="{StaticResource ThemeRadio}"
                                     Checked="SidebarRightRadio_Checked"/>
                    </StackPanel>

                    <TextBlock Text="{DynamicResource Str_ViewMode}" Style="{StaticResource SettingsSubheader}"
                               Margin="0,12,0,8"/>
                    <!-- View Mode picker: a flyout submenu, consistent with Theme/Language/Toolbar.
                         Stays open on selection so the user can try view modes back to back. -->
                    <Grid Margin="0,0,0,4">
                        <ToggleButton x:Name="ViewMenuButton"
                                      Checked="SettingsMenu_Toggled" Unchecked="SettingsMenu_Toggled"
                                      Cursor="Hand" FocusVisualStyle="{x:Null}"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="RowBd" Background="Transparent" Margin="-10,0,2,0" Padding="10,4,10,4" CornerRadius="3">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource BgHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="RowBd" Property="Background" Value="{DynamicResource SettingsOpenRowBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right"
                                           FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{DynamicResource TextSecondary}">
                                    <!-- Up chevron when the section is collapsed, down when expanded. -->
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xE70E;"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                    <Setter Property="Text" Value="&#xE70D;"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock x:Name="ViewCurrentLabel" Text="Continuous" Style="{StaticResource SettingsAccentValue}"/>
                            </DockPanel>
                        </ToggleButton>
                        </Grid>
                        <StackPanel x:Name="ViewSubmenu" Visibility="Collapsed" Margin="10,2,0,6">
                                    <RadioButton x:Name="ViewContinuousRadio"
                                                 Content="{DynamicResource Str_View_Continuous}"
                                                 GroupName="ViewGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ViewContinuousRadio_Checked"/>
                                    <RadioButton x:Name="ViewSingleRadio"
                                                 Content="{DynamicResource Str_View_Single}"
                                                 GroupName="ViewGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ViewSingleRadio_Checked"/>
                                    <RadioButton x:Name="ViewTwoPageRadio"
                                                 Content="{DynamicResource Str_View_TwoPage}"
                                                 GroupName="ViewGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ViewTwoPageRadio_Checked"/>
                                    <RadioButton x:Name="ViewGridRadio"
                                                 Content="{DynamicResource Str_View_Grid}"
                                                 GroupName="ViewGroup"
                                                 Style="{StaticResource ThemeRadio}"
                                                 Checked="ViewGridRadio_Checked"/>
                        </StackPanel>
                </StackPanel>
                </ScrollViewer>
                </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Keyboard shortcut overlay — neutral dark dim regardless of theme -->
        <Grid x:Name="ShortcutOverlay"
              Grid.RowSpan="5"
              Panel.ZIndex="10000"
              Visibility="Collapsed"
              Background="#88000000"
              MouseLeftButtonDown="ShortcutOverlay_MouseLeftButtonDown"
              MouseRightButtonDown="ShortcutOverlay_MouseLeftButtonDown">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="480" MaxWidth="640">
                <!-- Shadow cast by a separate element so the card content stays crisp. -->
                <Border Background="{DynamicResource BgModal}" CornerRadius="6" IsHitTestVisible="False">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="22" ShadowDepth="3" Direction="270" Opacity="0.55"/>
                    </Border.Effect>
                </Border>
                <Border Background="{DynamicResource BgModal}"
                        BorderBrush="{DynamicResource AccentBorder}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="28,16,28,22"
                        MouseLeftButtonDown="ShortcutOverlayCard_MouseLeftButtonDown"
                        MouseRightButtonDown="ShortcutOverlayCard_MouseLeftButtonDown">
                <Grid>
                <Border CornerRadius="6" Margin="-28,-16,-28,-22" IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}" Background="{StaticResource GrainBrushShared}"/>
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Str_KS_Title}"
                                   FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Click="ShortcutOverlayClose_Click"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                Width="24" Height="24" Content="&#xE711;"
                                Style="{StaticResource DangerCloseButton}"/>
                    </Grid>

                    <!-- Column layout -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left column -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{DynamicResource Str_KS_File}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+O" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_Open}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+S" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Save}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Shift+S" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_SaveAs}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+W" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_CloseFile}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+N" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_NewBlank}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+P" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_Print}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="{DynamicResource Str_KS_Tools}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="1 (or V)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Select}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="2 (or T)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Text}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="3 (or L)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Line}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="4 (or H)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Highlight}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="5 (or D)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Draw}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="6 (or I)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Image}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="7 (or G)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Signature}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="8 (or C)" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_Lbl_Crop}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="{DynamicResource Str_KS_Help}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="F1 / Ctrl+?" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ThisList}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="F2" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_About}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                        </StackPanel>

                        <!-- Right column -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{DynamicResource Str_KS_Navigation}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="&#x2190; / &#x2192; or &#x2191; / &#x2193;" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_PrevNext}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Scroll" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ZoomCursor}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+= / Ctrl+-" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ZoomInOut}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+0" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ResetZoom}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Middle drag" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_PanView}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Space + drag" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_PanView}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+B" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ToggleSidebar}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+Tab" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_NextTab}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="{DynamicResource Str_KS_Editing}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Z" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_Undo}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+C" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_CopyText}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+V" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_Paste}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Delete" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_DeleteAnnot}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Escape" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_ConfirmCancel}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="{DynamicResource Str_KS_SearchSelect}" Foreground="{DynamicResource Accent}"
                                       FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+F" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_Find}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Shift+Enter" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_NextPrevResult}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+A" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_SelectAll}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Shift+Click" FontFamily="Consolas" FontSize="11" Foreground="{DynamicResource TextSecondary}" Width="120"/><TextBlock Text="{DynamicResource Str_KS_MultiSelect}" FontSize="{DynamicResource Str_KS_FontSize}" Foreground="{DynamicResource TextPrimary}"/></DockPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
                </Grid>
                </Border>
            </Grid>
        </Grid>
        <!-- About overlay -->
        <Grid x:Name="AboutOverlay"
              Grid.RowSpan="5"
              Panel.ZIndex="10001"
              Visibility="Collapsed"
              Background="#88000000"
              MouseLeftButtonDown="AboutOverlay_MouseLeftButtonDown"
              MouseRightButtonDown="AboutOverlay_MouseLeftButtonDown">
            <Grid Width="560" VerticalAlignment="Center">
                <!-- Shadow is cast by a separate element so the card content stays crisp; a
                     DropShadowEffect rasterizes whatever it is applied to, which softens text. -->
                <Border Background="{DynamicResource BgModal}" CornerRadius="6" IsHitTestVisible="False">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="22" ShadowDepth="3" Direction="270" Opacity="0.55"/>
                    </Border.Effect>
                </Border>
                <Border Background="{DynamicResource BgModal}"
                        BorderBrush="{DynamicResource AccentBorder}"
                        BorderThickness="1"
                        CornerRadius="6"
                        MouseLeftButtonDown="AboutOverlayCard_MouseLeftButtonDown"
                        MouseRightButtonDown="AboutOverlayCard_MouseLeftButtonDown">
                <Grid>
                <!-- Film grain - sits on the card background only, behind the icon and text -->
                <Border CornerRadius="6" IsHitTestVisible="False" Opacity="{DynamicResource GrainOpacity}">
                    <Border.Background>
                        <ImageBrush x:Name="AboutGrainBrush" TileMode="Tile"
                                    ViewportUnits="Absolute" Viewport="0,0,256,256" Stretch="None"/>
                    </Border.Background>
                </Border>
                <Grid Margin="28,20,28,22">
                    <StackPanel Margin="0,0,20,0">
                    <!-- Header: bloody KillerPDF icon to the left of the title + tagline -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Image Source="pack://application:,,,/Resources/kp-icon.png"
                               Width="64" Height="64" Margin="0,0,16,0"
                               VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="3" Direction="270" Opacity="0.6"/>
                            </Image.Effect>
                        </Image>
                        <StackPanel VerticalAlignment="Center">
                            <!-- Logo (populated in code-behind), with a soft layered shadow -->
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="KillerPDF" FontSize="22" FontWeight="Bold"
                                           Foreground="#B0000000" Margin="1,2,0,0" IsHitTestVisible="False"
                                           Opacity="{DynamicResource IconShadowOpacity}">
                                    <TextBlock.Effect><BlurEffect Radius="2"/></TextBlock.Effect>
                                </TextBlock>
                                <TextBlock x:Name="AboutLogoBlock"
                                           FontSize="22" FontWeight="Bold" Cursor="Hand"/>
                            </Grid>
                            <!-- Tagline (populated in code-behind with hyperlinks) -->
                            <TextBlock x:Name="AboutTaglineBlock"
                                       FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                    <!-- Info card - matte panel lifted above the grainy card with a drop shadow.
                         Shadow is a separate element so the card's text stays crisp. -->
                    <Grid>
                    <Border Background="{DynamicResource BgPanel}" CornerRadius="4" IsHitTestVisible="False">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="14" ShadowDepth="2" Direction="270" Opacity="0.5"/>
                        </Border.Effect>
                    </Border>
                    <Border Background="{DynamicResource BgPanel}"
                            CornerRadius="4"
                            Padding="14,12,14,4">
                        <StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{DynamicResource Str_About_Version}" Foreground="{DynamicResource TextSecondary}"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" Margin="0,0,0,2"/>
                                <TextBlock x:Name="AboutVersionBlock"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="12"
                                           Foreground="{DynamicResource Accent}"
                                           Cursor="Hand"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{DynamicResource Str_About_Publisher}" Foreground="{DynamicResource TextSecondary}"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" Margin="0,0,0,2"/>
                                <TextBlock x:Name="AboutPublisherBlock"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="12"
                                           Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{DynamicResource Str_About_Thumbprint}" Foreground="{DynamicResource TextSecondary}"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" Margin="0,0,0,2"/>
                                <TextBlock x:Name="AboutThumbprintBlock"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="{DynamicResource TextPrimary}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{DynamicResource Str_About_ExeSha}" Foreground="{DynamicResource TextSecondary}"
                                           FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="11" Margin="0,0,0,2"/>
                                <TextBlock x:Name="AboutSha256Block"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="{DynamicResource TextPrimary}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    </Grid>
                    <!-- Update notice: shown only when a newer release is found (set in code-behind) -->
                    <Button x:Name="AboutUpdateButton" Visibility="Collapsed"
                            Margin="0,14,0,0" HorizontalAlignment="Left"
                            Cursor="Hand" FocusVisualStyle="{x:Null}"
                            Click="AboutUpdateButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="UpdBd" Background="Transparent"
                                        BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                                        CornerRadius="4" Padding="14,7">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="UpdBd" Property="Background" Value="{DynamicResource AccentDim}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock x:Name="AboutUpdateText" Text="Update available"
                                   FontFamily="Segoe UI, Microsoft JhengHei UI, Nirmala UI" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Accent}"/>
                    </Button>
                    </StackPanel>
                    <!-- X close button overlaid at top-right - no vertical space consumed -->
                    <Button Click="AboutOverlayClose_Click"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Width="24" Height="24" Content="&#xE711;"
                            Style="{StaticResource DangerCloseButton}"/>
                </Grid>
                </Grid>
                </Border>
            </Grid>
        </Grid>

    </Grid>
    </Border>
    </Grid>
</Window>
